<div class="container mb-5 mt-3">
  <h1 class="text-center">Edit <%= @contact.full_name %></h1>
</div>
<%= render 'form', contact: @contact, ecclesia: @ecclesia, phone_descriptions: @phone_descriptions%>
